# Backend Server URLs (Required for video calls in production)
# For local development, these default to localhost
# For production (Netlify), set these to your deployed backend URL

# Socket.IO Server URL (for signaling)
NEXT_PUBLIC_SOCKET_URL=http://localhost:3003

# PeerJS Server Configuration (for WebRTC)
NEXT_PUBLIC_PEER_HOST=localhost
NEXT_PUBLIC_PEER_PORT=9000
NEXT_PUBLIC_PEER_PATH=/myapp
NEXT_PUBLIC_PEER_SECURE=false

# Production Example (after deploying backend to Render/Railway):
# NEXT_PUBLIC_SOCKET_URL=https://your-app.onrender.com
# NEXT_PUBLIC_PEER_HOST=your-app.onrender.com
# NEXT_PUBLIC_PEER_PORT=443
# NEXT_PUBLIC_PEER_PATH=/peerjs/myapp
# NEXT_PUBLIC_PEER_SECURE=true
